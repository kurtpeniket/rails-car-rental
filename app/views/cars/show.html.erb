<div class="main-container">
  <div class="row">
    <div class="container col-6 show my-3">
      <div class="card car-details ml-5 ">
        <h1 class="mb-3"><%= @car.brand.upcase %></h1>
           <% if @car.img != nil %>
            <%= image_tag @car.img, alt: "alttext", class: "mx-auto d-block img-car"%>
          <% elsif @car.photo.attached? %>
            <%= cl_image_tag @car.photo.key, height: 500, width: 500, crop: :fill %>
          <% else %>
            <%= image_tag 'p1.jpg', alt: "alttext", class: "mx-auto d-block img-car"%>
          <% end %>
          <div class="bg-white">
            <h2 class="mt-4 text-center"><%= @car.model.upcase %></h2>
            <h4><i class="fas fa-tags mr-3"></i>£<%=@car.price_per_day %> per day</h4>
          </div>
        <div class="">
          <% if @car.user == current_user %>
          <%= link_to "Remove this car", car_path(@car), method: :delete, data: { confirm: "Confirm?" }, class: "btn btn-lg btn-outline-danger" %>
          <%= link_to "Edit this car", edit_car_path(@car), class: "btn btn-lg btn-outline-dark" %>
          <% end %>
        </div>
      </div>
    </div>

  <% unless @car.user == current_user %>
    <div class="col-6">
      <div class="bookings-card">
        <div class="booking row justify-content-center my-3">
          <div class="col-6 car-booking-form">
          <h2>Book this car</h2>
            <div class="form booking-form">
              <%= simple_form_for([@car, @booking]) do |f| %>
              <%= f.input :start_date, as: :date, html5: true %>
              <%= f.input :total_days %>
              <%= f.submit "Request booking", class: "btn-lg btn-block btn-outline-primary " %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
 </div>
</div>
